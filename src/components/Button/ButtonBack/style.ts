import { makeStyles } from '@mui/styles'
const useStyles = makeStyles((theme: any) => ({
  buttonBack: {
    color: 'red',
    fontSize: '30px',
    marginLeft: '100px',
  },
}))

export default useStyles
